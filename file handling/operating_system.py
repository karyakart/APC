import os

os.mkdir("Hello")#to make new folder
os.mkdir("next")
os.mkdir("sup")
print("new folder created")

print("list of folders created is as follows",os.listdir()) #ro show names of all files and folders